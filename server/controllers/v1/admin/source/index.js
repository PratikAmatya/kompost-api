const listSource = require("./list-source");
const createSource = require("./create-source");
const getSource = require("./get-source");
const deleteSource = require("./delete-source");
const updateSource = require("./update-source");

module.exports = {
  listSource,
  createSource,
  getSource,
  deleteSource,
  updateSource,
};
